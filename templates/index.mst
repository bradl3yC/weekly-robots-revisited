<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/main-style.css">
    <title></title>
  </head>
  <body>
    <ul>
      {{#robots}}
        <li class="users">
          <img src="{{avatar}}" alt="">
          <h2>
            <a href="/users/{{id}}">{{name}}</a>
          </h2>
            <p>{{job}} {{company}}</p>
        </li>
      {{/robots}}
    </ul>
  </body>
</html>
